#include <string.h>
#include <stdio.h>

size_t	strlcpy(char *restrict dst, const char * restrict src, size_t dstsize)
{
	if (size != 0)
	{
		while (src[i] && size i + 1 < size)
		{
			dst[i] = src[i];
			i++;
		}
		dst[i] = '\0';
	}
	return (ft_strlen(src));
}
